name: Quality Gate

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

jobs:
  quality:
    name: Typecheck, build and tests
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9.12.0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: WhatsApp Web settings security tests
        run: pnpm test:web-whatsapp-settings

      - name: WhatsApp Web BFF security tests
        run: pnpm test:web-whatsapp-bff

      - name: Typecheck
        run: pnpm typecheck

      - name: Lint
        run: pnpm lint

      - name: Build
        run: pnpm build

      - name: API tests
        run: pnpm test

      - name: Web navigation tests
        run: pnpm test:web-navigation

      - name: Route + navigation smoke (ürün öncesi kapı)
        run: pnpm smoke:routes && pnpm smoke:navigation

      - name: UI scope guard (PNG, leakage, fake copy)
        run: pnpm ui:guard

      - name: Secret pattern scan
        run: pnpm security:secret-scan

      - name: Dependency audit (high+)
        run: pnpm security:audit

      - name: WhatsApp Web BFF client bundle security scan
        run: pnpm security:web-whatsapp-bff-bundle

      - name: Upload quality logs on failure
        if: failure()
        uses: actions/upload-artifact@v7
        with:
          name: quality-gate-logs
          path: |
            .turbo/**/*.log
            apps/web/.next/**/*.log
            apps/web/.runtime-next/**/*.log
          if-no-files-found: ignore
