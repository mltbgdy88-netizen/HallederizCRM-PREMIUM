export interface StockToolbarProps {
  onActionClick?: (action: string) => void;
}

/** Eski devasa araç çubuğu kaldırıldı; üst çubukta kompakt aksiyonlar kullanılıyor. */
export function StockToolbar(_props: StockToolbarProps) {
  return null;
}
